// Feature flags. Flip a value to hide/show a whole section (its nav link + route)
// across the site in one place.
//   SHOW_EXPOSURE_RISK: the location -> pollutant -> respiratory-risk map (/air).
//   Set to false to hide it for now; set back to true to bring it back.
export const SHOW_EXPOSURE_RISK = true
